{
  "name": "web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "yarn run typecheck && yarn --production=true node build.js",
    "build:dev": "yarn run typecheck && node build.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.14.36",
    "typescript": "^4.6.3"
  }
}
